//-----------------------------------------------------------------------------
// File: GameSystem.h
//
// アプリ側のクラスに継承
// アプリ側で存在する単一のデバイスなどの参照を許可する
//-----------------------------------------------------------------------------
#pragma once
#include "main.h"
#include "GameSystem.h"
#include "ImGuiSystem.h"

// アプリに存在するデバイスなどの参照を許可する
class ApplicationInterface
{
public:

	/*
	void SetApplication(Application* application);
	void SetGameSystem(GameSystem* gameSystem);
	void SetImGuiSystem(ImGuiSystem* imGuiSystem);

protected:

	static Application* g_application;
	static GameSystem*  g_gameSystem;
	static ImGuiSystem* g_imGuiSystem;
	*/

};
